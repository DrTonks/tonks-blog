# 博客图片处理与管理流程（2026-09-09）

已在本地合入远程 261bf18，并修复图片处理问题。未部署前端、未提交 Git；用户原有 .gitignore 和 src/pages/about.astro 修改已保留。

## 管理方式

- 原图继续放 public/images；相册继续使用目录与 info.json；项目和时间线继续编辑 public/data/projects.json、timeline.json。
- Markdown/frontmatter 继续写原来的 /images/...；合法 YAML 注释、引号、多行标量可正常解析。贴纸中原来的 .jpg 路径仍可用。
- 构建为 albums/projects/covers 中适用的 PNG/JPEG 生成 WebP，仅选择更小的输出，保留原件和旧 URL。动画、未知和远程路径保留原行为；并非所有 Markdown 正文图片都自动转换。
- 缓存位于项目自己的 .cache/blog-images；不要提交缓存或 dist。public 原图仍应版本管理，不能仅凭审计清单删除。
- pnpm dev 下新增、替换、删除图片和修改管理 JSON/文章会更新清单并触发整页刷新；未保存的浏览器表单状态可能丢失。半写图片暂时不可解码时沿用最近成功结果，保存完成后恢复；真正删图会撤销对应衍生图。

## 修复与实现

统一 Windows 文件路径和 URL 的边界；正确处理中文、空格、编码、查询与片段。使用现有依赖中的 js-yaml 解析 frontmatter，无新增包或锁文件变更。修复启动扫描与 watcher 注册之间丢更新的问题，首次读取等待最新清单。监听重建串行，发布后同步失效 Vite 客户端/SSR 模块。保留存活图片当前及前一代 hash 供在途请求使用。

图片审计输出目录必须位于项目之外，验证实际路径和 junction，独占创建而不覆盖已有导出。解析 JSON/YAML 和动态相册引用；无法解码的图片记录诊断，不误判为可删除。审计不能证明外部旧链接或运行时引用不存在。

## 验证

图片生成、CDN 回退与审计测试共 8/8 通过。使用真实 Vite HTTP/HMR、文件变化及首扫间隙、半写恢复测试。独立复审发现的两个 P2 已修复并重新审查通过。

真实 Edge 下项目、相册、首页命中 WebP，旧原图 URL 仍返回 200。临时开启文章封面展示并加入合法 YAML 注释后，封面正常命中 WebP；测试后文章恢复原字节。

完整生产构建通过（Astro 47 页，后续字体和 Pagefind 步骤成功）。最终隔离构建 dist 为 77,268,370 B（73.69 MiB）、772 个文件；含全部输出共 214 个 WebP，HTML 中 163 个 src/srcset 属性指向 /_images/。源码已逐文件核对后复制回本地博客；未覆盖用户本地 dist。

旧版 dist 59,304,593 B（56.56 MiB）。增大主要是保留原图并新增优化图，不能据此推断单个访客下载增加；页面实际命中优化 URL 才会获得传输收益。原图兼容保留可后续按引用和外链证据优化，不急于删除。
