# 后续清理候选（本次不删除）

| 候选 | 依据 | 删除前需要确认 |
| --- | --- | --- |
| 番剧页与 Bangumi 配置 | `src/config.ts` 的 `featurePages.anime` 关闭 | 是否计划恢复；连同路由、翻译、请求逻辑一起评估 |
| 建设树 | `featurePages.construction` 明确为后续重做而保留 | 不能仅因关闭就删除数据；先获得维护者决定 |
| 动态侧栏实现 | `DynamicSideBar.astro` 与 `SideBar.astro` 并存 | 逐项检查导入者、响应式样式和 WidgetManager，不盲删共享模块 |
| 展示与诊断页面 | `src/pages/font-test.md` 等页面会参与生成 | 是否应该移到开发工具或独立示例，避免公开站点收录 |
| 字体与图片的历史素材 | `public/` 有源图、字体与生成资源 | 必须检查动态路径、兼容 URL、字体许可；源图可能是 WebP 构建输入 |
| 本地评论预览的目录假设 | `run-dev.mjs` 要求兄弟目录 `../sleepy` | 后续可增加明确的后端路径配置，改善 fork 在不同目录下的开发体验 |
| CI 验证范围 | 工作流只执行 `pnpm astro build` | 可独立 PR 增加 Python 依赖与完整 `pnpm build`，避免与功能删除混在一起 |

## 已补充完成

- 删除技能页及专用图表、配置、数据和翻译；历程技能标签保持不变。
- 将历史事故和执行记录归档，提供当前维护指南，保留原路径跳转说明。

## 本次刻意保留

- sleepy 的页面留言、文章和段落评论、访客身份及管理逻辑。
- 博客正文中介绍 Twikoo 或音乐系统的历史文章和随文图片。
- 图片自动优化、原图输入、友链本地头像与兼容路径。
- 文章加密、相册、RSS、Pagefind 等现有功能。

删除代码只能减少当前版本及部署产物的体积；不会自动清除 Git 历史中的模型文件。不需要为本次 PR 改写仓库历史。
