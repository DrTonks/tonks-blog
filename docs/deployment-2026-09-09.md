# 前端发布记录（2026-09-09）

最终效果：水平连续渐隐、无起伏和颗粒；渐隐区占底部遮罩高度 50%，响应式总高度不变。水波纹默认关闭，尊重访客已保存的显式选择。SVG 4,758 B。

## pnpm ship 实测

按顺序独立执行，耗时包含构建与部署，不包含此前的浏览器检查及复审。

| 项目 | 开始（北京时间） | 完成 | 总耗时 | 结果 |
| --- | --- | --- | ---: | --- |
| tonks-home | 14:11:51 | 14:12:30 | 38.82 秒 | 成功 |
| blogExample（首次） | 14:14:02 | 14:14:13 | 10.58 秒 | 构建失败，未上传 |
| blogExample（修复后） | 14:17:20 | 14:18:04 | 44.48 秒 | 成功 |

博客成功轮次分项：Astro 10.20 秒；字体子集 16.97 秒；Pagefind 0.83 秒；部署脚本 15.66 秒；其余为命令启动等开销。

博客 777 个文件，原始 74.16 MiB，压缩后 62.75 MiB；压缩 5.55 秒，上传 4.46 秒，远端校验与解压 3.69 秒，目录交换约 1.84 毫秒。当前与上一版压缩包由部署工具私有保留。

字体子集在此前独立构建验证中耗时 72.58 秒，成功 ship 中为 16.97 秒。此处为单次实测，受运行环境和缓存影响，不能将波动直接归因于上传，也不是严格性能基准。

## 构建修复

ImageWrapper.astro 原来使用 ../../** 导入整个 src，导致本地文章管理的 .base 文件被当作 JavaScript。改为只匹配图片扩展名，覆盖现有图片和大写 .JPG；本地管理文件未移动或删除。该修复经过独立子 agent 复审，完整生产构建通过。

## 验证和范围

- 水平渐隐完成 7 组本地 Edge 视口/主题检查与独立子 agent 复审。
- 主页与博客首页、博客 banner-edge-mask.svg 在线返回 HTTP 200，SHA-256 与本地 dist 文件一致。
- 主页发布至 /var/www/html；博客通过压缩包校验和目录交换发布至 /var/www/blog。
- 未修改 Apache 配置、未重启 PM2、未更新后端；未提交 Git。
- 既有访客曾主动开启水波纹时仍保留其偏好，需要手动关闭一次才能查看静态效果。
## 后续更正

用户发现博客生产版本严重样式错乱并已自行回退。上述构建/HTTP/哈希校验不代表视觉验收通过。根因及本地修复见 style-build-incident-2026-09-09.md；当前不得据此报告直接重发。
